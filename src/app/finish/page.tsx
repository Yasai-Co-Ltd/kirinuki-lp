'use client'

import Layout from '../../components/layout/Layout'

export default function Finish() {
  return (
    <Layout>
      <section>
        <h2 className="c">送信完了<span>Contact</span></h2>
        <p>送信が完了しました。</p>
      </section>
    </Layout>
  )
}